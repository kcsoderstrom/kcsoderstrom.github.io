# frozen_string_literal: true

source "http://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# gem "rails"

gem "jekyll", "~> 4.1"

gem "jekyll-admin", "~> 0.10.2", group: :jekyll_plugins
